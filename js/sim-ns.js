// Shared UI namespace. Each UI module attaches its section (SIM.UI,
// SIM.SETTINGS, SIM.STATS, SIM.PROFILES) — the module version of the old
// `var SIM = SIM || {}` script-tag pattern, kept so the extensive internal
// SIM.X.Y cross-references stay untouched.
export const SIM = {};
