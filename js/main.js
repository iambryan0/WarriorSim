// Vite entry point. For now it only carries the stylesheet through the build;
// the engine/UI scripts join the module graph file by file during the ESM
// conversion (see MIGRATION.md).
import '../scss/style.scss';
